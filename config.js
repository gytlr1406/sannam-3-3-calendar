// ─────────────────────────────────────────────────────────────
//  3-3 수시 면접·실기 일정 달력 설정
//  두 출처를 합쳐서 보여 줍니다. 둘 중 하나만 있어도 됩니다.
// ─────────────────────────────────────────────────────────────
window.CAL_CONFIG = {
  // ① 저장소 안의 일정 파일 (수시지원현황 + 대학별 면접일정표로 만든 것)
  //    엑셀로 열어 고친 뒤 CSV(UTF-8)로 저장해 올리면 반영됩니다.
  csvUrl: "일정.csv",

  // ② 구글폼 응답 시트 (선택)
  //    구글 시트를 [파일 → 공유 → 웹에 게시]에서 "쉼표로 구분된 값(.csv)"으로
  //    게시한 뒤, 그 주소를 따옴표 안에 붙여 넣으세요.
  //    (예: https://docs.google.com/spreadsheets/d/e/2PACX-.../pub?gid=0&single=true&output=csv)
  extraCsvUrl: "",

  // 달력 연도
  year: 2026,

  // 몇 분마다 자동으로 다시 불러올지 (0이면 끔)
  autoRefreshMinutes: 10,
};
